# GrapeHub — Environment Configuration
DATABASE_URL=sqlite:///./wine_cellar.db

# JWT
JWT_SECRET=grapehub-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# Uploads
UPLOAD_DIR=./uploads
MAX_UPLOAD_MB=10

# CORS — allow all origins for online deployment
CORS_ORIGINS=*
